**Breaking Changes**

* Added `snaptrade.link.mode` to the `PortfolioSchema` class.
* Reordered portfolio attributes in the `PorfolioSchema` class.

**New Features**

* Added the `PositionContainer.suspendCalculations` function.