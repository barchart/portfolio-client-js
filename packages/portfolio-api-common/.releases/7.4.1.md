**Bug Fixes**

* Updated `PositionItem` class to only apply bid/ask midpoint pricing to options.

**Technical Enhancements**

* Replaced usage of deprecated `Disposable.getIsDisposed` function with `Disposable.disposed` property.