**New Features**

* Added an optional attribute to the `TransactionSchema.BUY` and `TransactionSchema.SELL_SHOT` items. This attribute allows clients to directly specify the instrument's identifier (instead of the instrument's symbol).