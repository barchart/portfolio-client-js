**Bug Fixes**

* Fixed the issue occurred when a homogeneous position group became empty (length 0) during updates or deletion, as the condition `groupItems.length !== 0` incorrectly evaluated to true. Updated the logic to `groupItems.length > 0` to safely skip processing for empty groups.