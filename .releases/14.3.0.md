**New Features**

* Exposed aggregate dividend values through position items and groups.

**Technical Enhancements**

* Added a shared root ESLint configuration and `lint` and `lint:fix` scripts for the monorepo and individual packages.
