**Bug Fixes**

* Renamed the `TransactionType.dividends` property to `TransactionType.dividend` so dividend aggregation strategies can identify supported dividend and distribution transactions.

**New Features**

* Added the `snapshot.dividends` attribute to the complete and client `TransactionSchema` definitions.

\