**New Features**

* Added the `snapshot.dividends` attribute to some `TransactionSchema` definitions.
* Updated the `snapshot.dividends` attributes of `PositionSchema` to be optional.

**Other**

* Renamed the `TransactionType.dividends` property to `TransactionType.dividend` for asetetic purposes.
