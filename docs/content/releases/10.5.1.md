**Other**

* Replaced the `openOnly` argument on `PortfolioGateway.linkSnapTradeAccount` and `PortfolioGateway.refreshSnapTradeAccount` with a `mode` argument that sends the `SnapTradeLinkMode` code.
