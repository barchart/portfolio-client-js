**New Features**

* Added support for `openOnly` flag on `PortfolioGateway.linkSnapTradeAccount` and `PortfolioGateway.refreshSnapTradeAccount`, allowing consumers to choose between syncing only open positions or all positions.
